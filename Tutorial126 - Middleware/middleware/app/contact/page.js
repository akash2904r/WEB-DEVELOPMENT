import React from 'react'

const page = () => {
  return (
    <div className="text-center">
      <h1 className="font-bold text-3xl">Since you tried to access '/about' the content in '/contact' is rewritten in '/about' using the rewrite method through the middleware</h1>
    </div>
  )
}

export default page
